/**
 * @format
 */

import {AppRegistry} from 'react-native';
import BoostPlay from './src/BoostPlayApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => BoostPlay);
